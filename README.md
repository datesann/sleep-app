##起動方法　　
↓以下のURLから起動することができます。　　
https://datesann.github.io/sleep-app/

##テスト結果
| # | テスト対象 | テスト観点(正常/境界/異常) | テスト条件 | テスト手順(1行) | 期待値(1行) | 結果(○/×) |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 目標時間設定 | 正常 | 設定画面を開ける状態 | ⚙️設定から「⏰ 起床時間の設定」を開き「07:00」を入力して「目標を保存」を押す | 「現在の目標: 07:00」と表示され反映されること | 〇 |
| 2 | 目標時間設定 | 境界 | 目標が未入力のまま保存 | 「⏰ 起床時間の設定」で時刻を未入力(または空)にして「目標を保存」を押す | エラー終了せず、未設定状態として扱われる（または入力を促される）こと | × |
| 3 | 目標時間設定 | 正常 | 既に目標が設定されている | 「⏰ 起床時間の設定」を開き「目標を削除」ボタンを押す | 「現在の目標: 未設定」に戻り、保存内容が破棄されること | 〇 |
| 4 | 計測開始 | 正常 | 計測前画面(初期状態)にいる | 睡眠管理タブで「計測を開始する」ボタンを押す | 計測中画面に遷移し、下部タブと上部ボタンが無効化されること | 〇 |
| 5 | 計測終了 | 正常 | 計測開始から十分な時間経過 | 計測開始後、1分待ってから「計測終了」ボタンを押す | 計測後画面に遷移し、就寝・起床時間と睡眠時間が表示されること | 〇 |
| 6 | 計測終了 | 境界 | 計測開始から規定の最小時間(3秒)経過 | 計測開始からピッタリ3秒後に「計測終了」ボタンを押す | 正常に計測後画面へ遷移し、有効な記録として扱われること | 〇 |
| 7 | 計測終了 | 境界 | 計測開始から規定の最小時間未満 | 計測開始から2秒後に「計測終了」ボタンを押す | 「⚠️ 計測エラー」のモーダルが表示され、記録として保存されないこと | 〇 |
| 8 | 計測中断 | 正常 | 計測中画面にいる | 「計測中断」を押し、表示されるモーダルで「はい」を選ぶ | 計測が破棄され、計測前画面に戻ること | 〇 |
| 9 | 計測中断 | 異常 | 中断確認モーダル表示中 | 「計測中断」を押し、モーダル外側の背景をタップして閉じようとする | モーダルが不用意に閉じず、「いいえ」ボタンでのみ元の画面に戻れる(またはクラッシュしない)こと | 〇 |
| 10 | コメント入力 | 正常 | 計測後画面にいる | 「💬 今日の一言コメント」に「よく眠れた」と入力し「完了」を押す | 記録とコメントが保存され、計測前画面に戻ること | × |
| 11 | コメント入力 | 境界 | コメント未入力 | コメント欄を空欄にしたまま「完了」ボタンを押す | クラッシュせず、コメントなしの記録として正常に保存されること | × |
| 12 | コメント入力 | 境界 | 極端に長い文字数の入力 | コメント欄に1000文字の長文を入力して「完了」ボタンを押す | 文字数制限でカットされる、またはそのまま保存されても表示崩れやクラッシュが起きないこと | × |
| 13 | アプリ復帰 | 異常 | 計測中(During)にアプリ再起動 | 計測中にブラウザのリロードボタン（またはタスクキル）を実行し再度開く | 計測中状態から復帰する、または安全に計測前状態に戻りクラッシュしないこと | 〇 |
| 14 | カレンダー表示 | 正常 | カレンダータブにアクセス | 下部ナビの「カレンダー」ボタンを押し、カレンダー画面を表示する | 当月のカレンダーと平均データエリアが正しく表示されること | × |
| 15 | カレンダー月切替 | 正常 | カレンダータブにいる | 「◀ 前月」ボタンと「翌月 ▶」ボタンを複数回押して月を移動する | 月ごとのカレンダーがクラッシュせずに正しく切り替わること | 〇 |
| 16 | 平均データ表示 | 境界 | 睡眠記録が0件の初期状態 | アプリ初回起動時にカレンダータブの平均データを確認する | 直近7日・30日ともに「--時間 --分」など初期値のまま壊れずに表示されること | 〇 |
| 17 | 平均データ表示 | 境界 | 睡眠記録がちょうど7件ある | 開発者ツール等を使い過去7日間分のみの記録を作成し、平均データを見る | 直近7日間の睡眠・就寝・起床時間の平均が正しく計算され表示されること | 〇 |
| 18 | ショップ表示 | 正常 | ショップタブにアクセス | 下部ナビの「ショップ」ボタンを押す | 単色・2色・特殊テーマのカテゴリや入手済みアイテムのリストが表示されること | 〇 |
| 19 | ショップカテゴリ切替 | 境界 | ショップのカテゴリ両端 | カテゴリ切替の「◀」ボタンを連打して一番左端のカテゴリにする | 端のカテゴリでは「◀」ボタンが無効化(または非表示)され、エラーにならないこと | 〇 |
| 20 | テーマ購入 | 正常 | スタンプが価格分以上ある | 所持スタンプが十分な状態で、未入手テーマのボタンを押し「はい」を選ぶ | 購入が完了し、スタンプ数がテーマの価格分正しく減算されること | 〇 |
| 21 | テーマ購入 | 境界 | スタンプが価格とちょうど同じ | 所持スタンプを7にし、コスト7の単色テーマを購入する | 購入が成功し、所持スタンプが0になること | 〇 |
| 22 | テーマ購入 | 異常 | スタンプが不足している | 所持スタンプ0の状態で、コスト7のテーマの購入ボタンを押す | 「スタンプが足りません」等で安全に失敗し、購入処理やクラッシュが起きないこと | 〇 |
| 23 | テーマ適用 | 正常 | 購入直後の適用確認モーダル | テーマ購入後の「今すぐこの着せ替えに変更しますか？」で「はい」を選ぶ | 選択したテーマの配色(背景、ボタンの危険色等)に即座に切り替わること | 〇 |
| 24 | タブ移動ロック | 異常 | 計測中状態での他タブ遷移 | 計測中(During状態)に、下部ナビの「カレンダー」や「ショップ」を押す | ナビゲーションが無効化されており、別タブに遷移できず計測画面に留まること | 〇 |
